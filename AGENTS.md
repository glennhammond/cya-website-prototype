<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# CYA repository authority

- This repository, `glennhammond/cya-website-prototype`, is the only implementation authority for the CYA Website 2027.
- Base implementation work on the current qualified integration branch or its eventual `main` successor. Do not restart from an older prototype/default branch.
- `glennhammond/cya-site` is deprecated historical evidence only. Never write, merge, deploy or treat it as production authority.
- Production promotion, Squarespace replacement and DNS changes require Glenn's separate explicit approval.
- Preserve the authorised HubSpot planning form, Deb-only notification decision, campaign routes and conversion routes unless later governing evidence explicitly changes them.
- Governing authority order: Authoritative Site Architecture, Search Architecture Authority, current CYA brand/design authority, qualified production work, then supporting historical evidence.
